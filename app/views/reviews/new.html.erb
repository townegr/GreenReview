<%= simple_form_for @review do |f| %>
    <%= f.input :title, required: false %>
    <%= f.input :description, required: true %>
    <%= f.hidden_field :user_id, value: "#{current_user.id}" %>
    <%= f.association :product, label_method: :title %>
    <%= f.submit "Submit Review" %>
<% end %>
