<% @products.each do |product| %>

<%= image_tag product.image %>
  <ul>

    <% (1..5).each do |i| %>
      <%= rating(current_user, i, product) %>
    <% end %>

  </ul>

  <%= image_tag product.image %>

<% end %>
