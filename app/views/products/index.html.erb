<% @products.each do |product| %>
<%= image_tag product.image %>
<% end %>

<ul>
  <% (1..5).each do |i| %>
 <li class="rating_star" data-product-id="<%= product.id %>" data-stars="<%= i %>" id="<%="product_#{product.id}_#{i}"%>">star</li>
</ul>
<% end %>
